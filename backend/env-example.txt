# ============================================================================
# Configuración de Supabase API
# ============================================================================
# IMPORTANTE: Usa Supabase API directamente (no conexión directa a PostgreSQL)
# Según indicaciones del DBA: "backend_user YA NO SIRVE"

SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_ANON_KEY=tu-anon-key-aqui

# NOTA: SUPABASE_SERVICE_ROLE_KEY está prohibida por el DBA
# Para actualizar contraseñas en Auth, usar scripts/update-password-auth.js manualmente

# ============================================================================
# Configuración del Servidor
# ============================================================================
PORT=3001
NODE_ENV=development
DB_SCHEMA=joysense

# ============================================================================
# Notas:
# - SUPABASE_URL: URL de tu proyecto Supabase
# - SUPABASE_ANON_KEY: Anon key (NO service_role_key) - RLS funciona automáticamente
# - El backend usa tokens de sesión del frontend para todas las queries
# - Las políticas RLS usan auth.uid() para filtrar datos por usuario
# ============================================================================
